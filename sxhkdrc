# start a new terminal
alt + Return
  xterm

# close window
alt + q
  bspc node -c

# focus change
alt + Left
  bspc node -f west

alt + Down
  bspc node -f south

alt + Up
  bspc node -f north

alt + Right
  bspc node -f east
